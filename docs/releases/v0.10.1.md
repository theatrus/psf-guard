This patch release fixes database errors when opening a new, empty catalog.

## Fixed

- New, empty databases show zero image and grading counts instead of an
  `accepted` NULL database error. Empty projects also load their statistics.
- An empty image folder can be used to prepare a destination for NINA plugin
  sync without the overview failing. Existing databases do not need to be
  recreated or repaired.
