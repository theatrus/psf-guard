[package]
name = "psf-guard-director-ffi"
version = "0.2.0"
edition = "2024"
rust-version = "1.89"
license = "Apache-2.0"
publish = false
description = "Experimental caller-buffer C ABI for the Director planning core"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
psf-guard-director-core = { path = "../director-core" }
serde_json = "1"
