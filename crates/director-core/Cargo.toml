[package]
name = "psf-guard-director-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
license = "Apache-2.0"
publish = false
description = "Experimental deterministic goal selection for PSF Guard Director"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
